{
  "name": "@starryworks_inc/step-series",
  "version": "0.1.3",
  "description": "node.js library to use ponoor experiment's STEP series(STEP 800/400).",
  "main": "dist/index.js",
  "files": [
		"dist/"
	],
  "types": "dist/index.d.ts",
  "directories": {
    "example": "examples",
    "test": "test"
  },
  "scripts": {
    "build": "npx tsc",
    "npm:publish": "npm publish --access=public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "STARRYWORKS inc.",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@types/node": "^17.0.13"
  }
}
